# automation-ninja -- Docker image for automation labs
(Network) automation docker images


Installed packages:
```bash
python3, htop, net-tools, curl, wget, vim, dnsutils, iproute2, iputils-ping, traceroute, openssh-client, git
```

